class TableCreateModel {
    constructor(name, restaurant, numberOfPlaces) {
        this.name = name
        this.restaurant =restaurant
        this.numberOfPlaces = numberOfPlaces
    }
}

module.exports = TableCreateModel
