class ScoreModel {
    constructor(id, paymentToken, paymentAmount) {
        this.id = id
        this.paymentToken = paymentToken
        this.paymentAmount = paymentAmount
    }
}

module.exports = ScoreModel
